import { makeStyles } from "@material-ui/core";

const useStyle = makeStyles((theme) => ({
  circle: {
    borderRadius: "50%",
    height: "100px",
    width: "100px",
  },
}));

export default useStyle;
